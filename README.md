# Налоговый кодекс Республики Казахстан — машинно-читаемый корпус

**Редакция 2026-01-01** · 849 статей · 3 147 чанков · снято 17.08.2026 · проверка целостности: PASS

Полный текст Налогового кодекса РК (кодекс от 2025 года, введён в действие с 1 января 2026 года)
в виде, пригодном для LLM-ассистентов, RAG-поиска и coding-агентов: одна статья — один Markdown-файл
со стабильным frontmatter, плюс производные артефакты для поиска.

> [!warning] Это **неофициальная** копия. Для юридически значимых решений — эталонный контрольный банк
> НПА и ИПС «Әділет». Подробности — [DISCLAIMER.md](DISCLAIMER.md).

## Что внутри

| Слой | Что это | Где |
|---|---|---|
| Человек и агент | 849 статей Markdown + YAML-frontmatter, anchor на каждый пункт | [`code/ru/`](code/ru/) |
| Навигация | полное дерево «часть → раздел → глава → параграф → статья» | [`index.md`](index.md), [`llms.txt`](llms.txt) |
| Машина | чанки по пунктам статей с контекстными заголовками | [`data/chunks.jsonl`](data/chunks.jsonl) |
| Контроль | редакция, счётчики, sha256 по каждому файлу, аномалии источника | [`manifest.json`](manifest.json) |
| Оценка | эталонные вопросы и честный baseline-прогон | [`eval/`](eval/) |
| Происхождение | оглавление и манифест исходной выгрузки | [`provenance/`](provenance/) |

Markdown в `code/ru/` — **источник истины**. `chunks.jsonl`, `tax.sqlite` и любые эмбеддинги —
производные, воспроизводимые скриптами из `scripts/`.

## Быстрый старт

**1. Как локальная база знаний для Claude Code / Codex**

```bash
git clone https://github.com/bronxtc52/nalogovyy-kodeks-2025.git
# статьи читаются как обычные файлы: code/ru/**/st-NNN.md, оглавление — index.md
```

**2. Как хранилище Obsidian**

Откройте каталог репозитория как vault: ссылки, callout-блоки и frontmatter-теги уже расставлены.

**3. Локальный полнотекстовый поиск за минуту (без зависимостей)**

```bash
python3 scripts/build_sqlite.py                 # dist/tax.sqlite, FTS5, ~12 МБ
sqlite3 dist/tax.sqlite "
  SELECT c.article_label, c.point, snippet(chunks_fts, 1, '[', ']', '…', 10)
  FROM chunks_fts JOIN chunks c ON c.rowid = chunks_fts.rowid
  WHERE chunks_fts MATCH 'счет-фактура AND экспедитор'
  ORDER BY bm25(chunks_fts) LIMIT 5;"
```

**4. Как корпус для своего RAG**

```python
import json
chunks = [json.loads(line) for line in open("data/chunks.jsonl", encoding="utf-8")]
# у каждого чанка: text, context_header, path, article, point, effective_from, source_url
```

Эмбеддинги не входят в git-историю: они появятся артефактом релиза в версии 0.2.0
(см. [дорожную карту](#дорожная-карта)).

## Модель данных

Одна статья — один файл `code/ru/<часть>/<раздел>/<глава>/st-NNN.md`. Frontmatter даёт фильтры
без парсинга текста: `article`, `heading`, `part`, `razdel`, `glava`, `paragraf`, `path`,
`revision_id`, `effective_from`, `effective_to`, `status`, `cross_references`, `source_url`,
`source_text_sha256`, `text_sha256`. Полное описание полей — [SCHEMA.md](SCHEMA.md).

Внутри статьи каждый пункт адресуем якорем: `code/ru/.../st-190.md#p-3` — это пункт 3 статьи 190.
Чанки нарезаны по той же границе (`chunk_id` вида `kz-tax-code-2025/ru/st-190/p-3`), поэтому
ассистент может дать цитату со ссылкой до пункта, а не «где-то в статье».

### Даты введения в действие

Кодекс введён в действие с 1 января 2026 года (статья 848). Исключения кодируются в
`effective_from` / `effective_note` по тексту той же статьи 848:

| Норма | Введение в действие |
|---|---|
| статья 189 | 1 июля 2026 |
| статья 92, глава 90 | 1 января 2027 |
| пункты 2 и 3 статьи 391 | 1 января 2028 (пометкой `effective_note`) |
| подпункт 1) пункта 3 статьи 351 | 1 января 2030 (пометкой `effective_note`) |

## Что этот корпус НЕ содержит

Честные границы важнее полноты обещаний:

- **нет казахской версии** — только русский текст (`lang: ru`); казахская зеркальная ветка запланирована на 0.2.0;
- **нет подзаконных актов** — приказы, правила и разъяснения КГД сюда сознательно не смешаны;
- **нет истории редакций** — снят один срез; будущие изменения будут приходить коммитами и тегами `revision/*`;
- **нет эмбеддингов** — только текст и полнотекстовый индекс;
- **нет юридической силы** — см. [DISCLAIMER.md](DISCLAIMER.md).

## Известные особенности источника

Всё перечисленное — свойства исходной публикации, а не дефекты сборки; каждое отражено
в `manifest.json → anomalies` и в самих файлах:

- **статья 419 существует в двух экземплярах** (разные идентификаторы источника): `st-419.md`
  и `st-419--2.md`. У второй источник отдаёт пустое тело — файл сохранён с явной пометкой
  `status: source_empty`, чтобы пропуск нельзя было принять за потерю при выгрузке;
- **у раздела 6** (индивидуальный подоходный налог) в оглавлении источника на месте названия
  стоит пояснительная сноска: номер восстановлен по позиции между разделами 5 и 7,
  `razdel_title` остаётся `null` с флагом `razdel_title_missing_in_source`;
- **20 статей (201–220)** идут в Особенной части до первого раздела — у них `razdel: null`,
  каталог `razdel-00-vne-razdelov`;
- **у 6 статей** (354, 442, 469, 666, 725, 832) заголовок из API источника расходится с оглавлением;
  верным взят заголовок оглавления, расхождение помечено флагом `source_title_mismatch`.

## Оценка качества

В `eval/qa-ru.jsonl` — 53 размеченных вопроса двух видов: 12 сформулированных как вопросы
бухгалтера и 41 выведенный из заголовков статей (по построению лёгкий, нужен как регрессия).
Baseline-прогон — обычный BM25 по FTS5, без эмбеддингов и реранкера:

| Набор | recall@1 | recall@5 | recall@10 | recall@20 |
|---|---|---|---|---|
| вопросы бухгалтера (12) | 0.417 | 0.750 | 0.917 | 0.917 |
| выведенные из заголовков (41) | 0.780 | 0.805 | 0.878 | 0.927 |

Это **пол, а не витрина**: цифры показывают, что даёт голый лексический поиск. Разбор промахов
и методика — [eval/RESULTS.md](eval/RESULTS.md). Векторный прогон появится вместе с эмбеддингами.

## Воспроизводимость

```bash
python3 scripts/build_corpus.py <путь-к-выгрузке>   # выгрузка → code/ru, index.md, manifest.json
python3 scripts/build_chunks.py                     # code/ru → data/chunks.jsonl
python3 scripts/build_sqlite.py                     # chunks → dist/tax.sqlite (FTS5)
python3 scripts/build_eval_seed.py                  # eval/qa-ru.jsonl
python3 scripts/eval_fts.py                         # baseline-прогон → eval/results-fts.json
python3 scripts/validate.py                         # 15 проверок целостности; ненулевой код = не публиковать
```

Скрипты используют **только стандартную библиотеку Python 3.11+** — ни `pip install`, ни PyYAML не нужны.
`scripts/validate.py` гоняется в CI на каждый PR.

## Дорожная карта

- **v0.1.0** (сейчас) — русский текст, Markdown + index + llms.txt + manifest + chunks.jsonl + eval + CI.
- **v0.2.0** — казахская версия, эмбеддинги BGE-M3 и `tax.sqlite` с векторами в GitHub Releases, векторный прогон бенчмарка.
- **v0.3.0** — MCP-сервер (`search_articles`, `get_article(article, on_date)`, `diff_revisions`), `related/` с подзаконными актами.
- далее — теги `revision/<дата>` на каждую редакцию, чтобы `git diff revision/2026-01-01..revision/2026-07-01` показывал изменение нормы.

## Лицензии и источник

- **[LICENSE](LICENSE)** — MIT: распространяется на скрипты, схему и любую обвязку в этом репозитории.
- **[LICENSE-DATA](LICENSE-DATA)** — CC0 1.0: распространяется на разметку, структуру и метаданные корпуса.
- **Сам текст кодекса** авторским правом не охраняется: статья 8 Закона РК «Об авторском праве и
  смежных правах» исключает официальные документы законодательного характера из объектов авторского права.
- Текст получен постатейно через штатный API портала [Учет.kz](https://new.uchet.kz) (доступ оплачен
  владельцем репозитория) и переработан в Markdown; HTML-страницы, оформление и редакционные
  комментарии портала здесь **не воспроизводятся** — только текст норм. Ссылка на источник стоит
  в каждой статье.

## Как сообщить об ошибке

Расхождение с официальным текстом — issue с номером статьи и пунктом. Ошибка в разметке
(иерархия, якорь, перекрёстная ссылка) — issue или PR: `scripts/validate.py` должен оставаться зелёным.
